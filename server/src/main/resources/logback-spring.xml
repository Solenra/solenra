<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="LOGS" value="${catalina.base}/logs" />

    <appender name="Console"
              class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{ISO8601} [%thread] %-5level %logger{36} - %msg%n%throwable</pattern>
        </encoder>
    </appender>

    <!-- LOG everything at INFO level -->
    <root level="INFO">
        <appender-ref ref="Console" />
    </root>

    <!-- LOG "com.github.solenra*" at DEBUG level -->
    <logger name="com.github.solenra" level="DEBUG" additivity="false">
        <appender-ref ref="Console" />
    </logger>

    <!-- LOG "org.springframework.retry*" at INFO level -->
    <logger name="org.springframework.retry" level="INFO" />

    <springProfile name="local">
        <!-- LOG to console when running in local environment -->
        <root level="INFO">
            <appender-ref ref="Console" />
        </root>
        <logger name="com.github.solenra" level="DEBUG">
            <appender-ref ref="Console" />
        </logger>
    </springProfile>

</configuration>